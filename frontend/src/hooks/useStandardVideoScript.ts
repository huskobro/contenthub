// Re-exports the script query hook from the shared detail hooks file.
// Kept as a separate file per phase 16 structure requirements.
export { useStandardVideoScript } from "./useStandardVideoDetail";
