# Database session setup — Phase 1 placeholder.
#
# SQLite engine with WAL mode, SQLAlchemy async session factory,
# and dependency injection helper will be added here in Phase 2
# when the settings registry and first models are introduced.
#
# Dependencies to add then: sqlalchemy[asyncio], aiosqlite, alembic.
